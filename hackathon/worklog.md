# Worklog

## 2026-09-14

- Confirmed the local MoonBit toolchain and initialized skeleton state without reinitializing the project.
- Verified the read-only remote reference root is visible. Metadata recursion encountered access-denied historical `archive/.../repo_snapshot/policy` directories; direct inspection confirmed the parent is listable but those child directories cannot be enumerated in the sandbox. They were not read or changed.
- Implemented and executed the first JSON-to-audit-to-Markdown vertical slice in MoonBit.
- Added synthetic and anonymized fixtures only; no remote artifacts or private paths were copied.
- Verified `moon fmt`, `moon info`, `moon check --target native`, `moon test --target native`, `moon build --target native`, and CLI demo commands locally.
- Read one formal evaluation JSON only to extract a SHA-256, field names, and aggregate schema evidence; updated aliases for `summary.episode_count`, `config.timeout_action_steps`, and `episode_attempt_index`. No raw content was committed.
- Added Wilson 95% intervals, deterministic JSON reports, protocol fingerprints, safe compare behavior, CLI aliases for the required command names, and public-API/boundary tests. Native test total reached 31/31 passing.
- Added JSONL and simple CSV adapters, action-step statistics, failure/skipped distributions, stage funnels, public canonical model types, all RA001-RA015 checks, deterministic CSV reports, binary-safe pure MoonBit SHA-256, sorted manifests, and direct file verification. Native test total reached 77/77 passing, including single-record JSONL and missing-index checks.
- Ran native release benchmarks on synthetic 1,000-episode input: validate 2.46 ms +/- 125.79 us; JSON report 3.05 ms +/- 250.48 us in this run.
- `moon package --list` completed and produced the local package ZIP. `moon publish --dry-run` completed its check/package phase but stopped because Mooncakes credentials are absent; no publication occurred.
- Created a fresh local clone from commit `91a1ee1662bb88f5b129b917eb8bceed9bdaa4c9`. The clone resolved the pinned `moonbitlang/async@0.21.3` dependency from the Moon cache, then passed `moon fmt --check`, `moon info`, native check, all 77 tests, native build, and `moon package --list`. The only compiler warning was an upstream async/Windows SDK `EINVAL` macro redefinition; all commands exited successfully.
- The read-only reference drive later became unavailable in the execution session. Ten-task metadata audit remains pending; local work continued without requesting credentials or touching remote state.
- Corrected RA015 to its specified provenance/code-revision semantics; added canonical `code_revision`, `seed_policy`, `environment`, and `provenance` fields across adapters, fingerprints, and reports. Added CLI `--version` plus exit codes 1 for audit findings and 2 for invalid commands. Native test total reached 81/81.
- Added stable per-rule finding paths and remediation suggestions plus an explicit RA001-RA015 positive/negative test matrix. Native test total reached 87/87.
- Restored read-only access to the reference drive and completed the usable ten-task metadata inventory without opening videos or copying raw artifacts. Nineteen 100-episode result leaves were counted across ten task names; each leaf had 100 unique seeds and the same seed-sequence digest. Protected historical snapshots and three output roots remained ACL-inaccessible and were recorded as limitations rather than inferred evidence.
- Inspected the nearest formal delivery metadata for `items_handover` using field names, counts, hashes, and aggregate statistics only. Recorded source-relative paths and SHA-256 values for evaluation metrics, formal audit/delivery metadata, failure analysis, manifest, delivery, and reproduction documents in `docs/reference-evidence.md`; no machine-private absolute paths or raw media entered the repository.
- Extended the canonical model with schema version, policy, seed policy, environment, provenance, code revision, duration, and optional run-level failure annotations. Reports now include failure proportions; compatible comparisons include absolute/relative success changes and failure-category deltas; protocol fingerprints cover the new protocol fields.
- Added `report --format markdown|json|csv`, incompatible-protocol and skipped/retry fixtures, non-finite-number rejection tests, generated Wilson-bound checks, and deterministic JSON/Markdown golden SHA-256 tests. Native test total reached 97/97 before final release-gate rerun.
- Re-ran release benchmarks after the canonical/report changes: validate synthetic 1,000-episode JSON averaged 2.82 ms +/- 175.66 us over 10 x 36 runs; JSON report generation averaged 2.92 ms +/- 176.45 us over 10 x 31 runs. These are development-machine observations, not universal claims.
- Created local commits `af7aa4e41ea5d138ee30eb9fc01b8ccd978d4a1b` and `185bc4f318a669a382bf11d5b176980643e82202`. A fresh `--no-hardlinks` clone of the latter passed format, interface generation, native check, 97/97 tests, native build, and package listing, then remained content-clean. Added an LF rule for generated `*.mbti` files after an earlier clone exposed a Windows `core.autocrlf`-only status change with an empty normalized diff.
- Added a one-command PowerShell demo and a submission requirements matrix. Running the demo exposed an RA008 false positive for a legitimate accepted candidate after a disclosed skipped candidate. Updated the RoboSyn adapter to normalize `feasibility_filter.skipped_seeds`, recognize enabled feasibility filtering as retry disclosure, and bound candidate indices against completed plus skipped candidates. The anonymized fixture now mirrors that real shape; the accessible formal metrics file produces only the expected RA006/RA015 disclosures. Native tests reached 100/100.
- Re-ran release benchmarks after the feasibility adapter fix: validate averaged 2.82 ms +/- 142.40 us over 10 x 27 runs; JSON report generation averaged 3.04 ms +/- 191.18 us over 10 x 31 runs.
- Cloned commit `9ab064b4ee2dca26c192ac7207a3d2db5ed88fce` with `--no-hardlinks` into another clean directory. Format checking, interface generation, native check, 100/100 tests, native build, the complete runnable demo, and package listing passed; Git status and normalized diff remained clean afterward.
